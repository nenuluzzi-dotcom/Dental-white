services:
  - type: web
    name: dental-white
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: SUPABASE_URL
        value: https://xejfkrzaofqjzvzjyovh.supabase.co
      - key: SUPABASE_KEY
        sync: false
